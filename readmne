const btn = document.querySelector('#btn');

setTimeout(() => {
  // код, выполняющийся в течение 6 мс
}, 10);

setInterval(() => {
  // код, выполняющийся в течение 8 мс
}, 10);

btn.addEventListener('click', () => {
  // код, выполняющийся в течение 10 мс
});

// код, выполняющийся в течение 18 мс


// произошел клик через 6 мс после инициализаации приложения
